# TASK — /dev/ui Reference Reconstruction & Final Visual Fidelity Pass

Depends on: Tasks 2–11 and `docs/ui/VISUAL_FIDELITY_SPEC.md`.

## Status

Implemented. Route: `/dev/ui`.

## What changed

`/dev/ui` is no longer only a component catalogue.

- **Part B** — Generic Game Composition: full shell (top bar, nav, records workspace, chat, activity) with fake data. Not Equipment, Market, or Combat.
- **Part A** — Component Reference: Typography, Colors, Surfaces, Buttons, Tabs, Inputs, Progress, Badges, Rows, Tooltips, States.

Visual QA of the engine as a whole is against `docs/mockups/*.png`.

Office Mode keeps short contact shadows on raised/floating plates.
