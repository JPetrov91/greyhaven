spring:
  jpa:
    show-sql: false
  flyway:
    enabled: true

server:
  servlet:
    session:
      cookie:
        secure: false

logging:
  level:
    root: WARN
    com.example.game: INFO

greyhaven:
  diagnostics:
    enabled: true
